---
'@bem/sdk.naming.cell.pattern-parser': major
---

Migrated to TypeScript with named export `patternParser` (default export retained).
Package now ships ESM-only with `dist/index.{js,d.ts}`.
Minimum Node bumped to >=20.
