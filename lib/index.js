'use strict';

const read = require('./read');
const parse = require('./parse');
const load = require('./load');
const resolve = require('./resolve');

module.exports = { load, read, parse, resolve };
