---
'@bem/sdk.bemjson-to-decl': major
---

Migrated to TypeScript / ESM (Node >=20). Bumped `stringify-object` to 6.0.0
(ESM-only). Public API: named exports `convert(bemjson, ctx)` and
`stringify(bemjson, ctx, opts)`.
